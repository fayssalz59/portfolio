---
interface Props {
  label: string;
  title: string;
  body?: string;
}

const { label, title, body } = Astro.props;
---

<div class="section-header">
  <span>{label}</span>
  <h2>{title}</h2>
  {body && <p>{body}</p>}
</div>
